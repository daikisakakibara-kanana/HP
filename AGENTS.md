# AGENTS.md

## Cursor Cloud specific instructions

This repository is currently empty (single `README.md` with `# HP`). There are no applications, services, dependencies, tests, or build configurations to run.

- **No update script dependencies**: The update script is a no-op (`echo "No dependencies to install"`). Update it when the first real dependency or project structure is added.
- **No services to start**: When code is added, document startup instructions here.
- **No tests or lint**: When a test framework or linter is added, document how to run them here.
